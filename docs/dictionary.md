# Data dictionary

Generated from `src/vra/registry.py` by `uv run vra dictionary`. Do not edit by
hand: the registry is the source of truth, and this file is a rendering of it.
Definitions are given in English and Portuguese; `aggregation` says what happens
to the column when rows are rolled up to a coarser grain (`sum` adds, `recompute`
must be rebuilt from the flights, `none` is a key or label).

## Staged flights (`data/staged/year=YYYY/part-0.parquet`)

31 columns.

| column | type | unit | aggregation | definition (en) | definição (pt) |
|---|---|---|---|---|---|
| `flight_date` | date32 | date | none | Calendar date of the scheduled departure; falls back to the actual departure date when the schedule is missing. | Data civil da partida prevista; usa a partida real quando a prevista está ausente. |
| `year` | int16 | year | none | Calendar year of flight_date. | Ano civil de flight_date. |
| `month` | int8 | month | none | Calendar month of flight_date, 1-12. | Mês civil de flight_date, 1-12. |
| `ym` | int32 | YYYYMM | none | Year-month key as YYYYMM, computed in 32-bit arithmetic. | Chave ano-mês no formato AAAAMM, calculada em aritmética de 32 bits. |
| `airline` | string | ICAO designator | none | Three-letter ICAO designator of the operating airline. | Designador ICAO de três letras da empresa operadora. |
| `flight_number` | int32 | count | none | Flight number as an integer; leading zeros of the 2010-2013 layout are dropped. | Número do voo como inteiro; zeros à esquerda do layout 2010-2013 são removidos. |
| `line_type` | string | code | none | IAC 1504 line type: I international, N national, R regional, E special, L, H, C, G. | Tipo de linha da IAC 1504: I internacional, N nacional, R regional, E especial, L, H, C, G. |
| `di` | int8 | code | none | IAC 1504 authorisation code (DI), 0-9; the letters A and B are mapped to 10 and 11. | Código de autorização (DI) da IAC 1504, 0-9; as letras A e B viram 10 e 11. |
| `origin_icao` | string | ICAO code | none | ICAO code of the origin aerodrome, as published. | Código ICAO do aeródromo de origem, como publicado. |
| `dest_icao` | string | ICAO code | none | ICAO code of the destination aerodrome, as published. | Código ICAO do aeródromo de destino, como publicado. |
| `origin_node` | string | node code | none | Origin node (ADR-0001): MRSP for SBSP/SBGR/SBKP, MRRJ for SBGL/SBRJ, MRBH for SBBH/SBCF; every other airport keeps its ICAO code. | Nó de origem (ADR-0001): MRSP para SBSP/SBGR/SBKP, MRRJ para SBGL/SBRJ, MRBH para SBBH/SBCF; os demais aeroportos mantêm o próprio ICAO. |
| `dest_node` | string | node code | none | Destination node, under the same rule as origin_node. | Nó de destino, pela mesma regra de origin_node. |
| `route` | string | node pair | none | Directional route key, origin_node-dest_node. | Chave direcional da rota, origin_node-dest_node. |
| `status` | string | category | none | Normalised flight status: realized, cancelled or other. | Situação do voo normalizada: realized, cancelled ou other. |
| `cause_code` | string | code | none | Two-letter IAC 1504 justification code; null when absent or N/A. For 2010-2013 the raw file carries the free text, which is mapped back to the code. | Código de justificativa de duas letras da IAC 1504; nulo quando ausente ou N/A. Em 2010-2013 o arquivo bruto traz o texto, que é remapeado para o código. |
| `sched_dep` | timestamp[s] | local time (Brasília) | none | Scheduled departure timestamp as published, in Brasília local time. | Partida prevista como publicada, no horário de Brasília. |
| `actual_dep` | timestamp[s] | local time (Brasília) | none | Actual departure timestamp; null when the flight did not depart. | Partida real; nula quando o voo não partiu. |
| `sched_arr` | timestamp[s] | local time (Brasília) | none | Scheduled arrival timestamp as published. | Chegada prevista como publicada. |
| `actual_arr` | timestamp[s] | local time (Brasília) | none | Actual arrival timestamp; null when the flight did not arrive. | Chegada real; nula quando o voo não chegou. |
| `dep_delay_min` | float32 | minute | mean | Signed departure delay, actual_dep minus sched_dep; negative when early, null when either timestamp is missing (ADR-0008). | Atraso de partida com sinal, actual_dep menos sched_dep; negativo quando antecipado, nulo quando falta um dos horários (ADR-0008). |
| `arr_delay_min` | float32 | minute | mean | Signed arrival delay, actual_arr minus sched_arr; early arrivals stay negative (ADR-0008). | Atraso de chegada com sinal, actual_arr menos sched_arr; chegadas antecipadas permanecem negativas (ADR-0008). |
| `sched_block_min` | int16 | minute | mean | Scheduled block time, sched_arr minus sched_dep. | Tempo de bloco previsto, sched_arr menos sched_dep. |
| `actual_block_min` | int16 | minute | mean | Actual block time, actual_arr minus actual_dep; null for flights that did not both depart and arrive. | Tempo de bloco realizado, actual_arr menos actual_dep; nulo para voos que não partiram e chegaram. |
| `dep_hour` | int8 | hour | none | Hour of the scheduled departure, 0-23. | Hora da partida prevista, 0-23. |
| `arr_hour` | int8 | hour | none | Hour of the scheduled arrival, 0-23. | Hora da chegada prevista, 0-23. |
| `dow` | int8 | weekday | none | Day of week of flight_date, 0 Monday to 6 Sunday. | Dia da semana de flight_date, 0 segunda a 6 domingo. |
| `group` | string | group code | none | Airline economic group at the flight date, from data/external/groups.csv (ADR-0003); null when the table is absent at staging time. | Grupo econômico da empresa na data do voo, de data/external/groups.csv (ADR-0003); nulo quando a tabela não existe na hora do staging. |
| `class` | string | class | none | Business-model class of the group at the flight date: FSC, LCC or regional (ADR-0003). | Classe de modelo de negócio do grupo na data do voo: FSC, LCC ou regional (ADR-0003). |
| `universe_repl` | bool | flag | sum | Replication universe (ADR-0002): line_type in N, R, E and di equal to 0; realised and cancelled flights both included. | Universo de replicação (ADR-0002): line_type em N, R, E e di igual a 0; realizados e cancelados incluídos. |
| `universe_ml` | bool | flag | sum | Prediction universe (ADR-0002): universe_repl restricted to realised flights. | Universo de previsão (ADR-0002): universe_repl restrito aos voos realizados. |
| `is_realized` | bool | flag | sum | True when the published status is REALIZADO. | Verdadeiro quando a situação publicada é REALIZADO. |

## Fact table, group x route x month (`data/analysis/fact_group_route_month.parquet`)

87 columns.

| column | type | unit | aggregation | definition (en) | definição (pt) |
|---|---|---|---|---|---|
| `ym` | int32 | YYYYMM | none | Year-month key. | Chave ano-mês. |
| `year` | int16 | year | none | Calendar year. | Ano civil. |
| `month` | int8 | month | none | Calendar month, 1-12. | Mês civil, 1-12. |
| `route` | string | node pair | none | Directional route, origin_node-dest_node (ADR-0001). | Rota direcional, origin_node-dest_node (ADR-0001). |
| `origin_node` | string | node code | none | Origin node (ADR-0001). | Nó de origem (ADR-0001). |
| `dest_node` | string | node code | none | Destination node (ADR-0001). | Nó de destino (ADR-0001). |
| `group` | string | group code | none | Airline economic group at this month (ADR-0003); an unlabelled airline keeps its own ICAO code. | Grupo econômico da empresa neste mês (ADR-0003); empresa sem rótulo mantém o próprio ICAO. |
| `class` | string | class | none | Business-model class: FSC, LCC, regional or other (ADR-0011). | Classe de modelo de negócio: FSC, LCC, regional ou other (ADR-0011). |
| `flights` | int32 | flights | sum | Flights of the replication universe scheduled in the cell: realised plus cancelled (ADR-0002). This is the article's `f`. | Voos do universo de replicação programados na célula: realizados mais cancelados (ADR-0002). É o `f` do artigo. |
| `realized` | int32 | flights | sum | Flights of the cell that operated. | Voos da célula que operaram. |
| `cancelled` | int32 | flights | sum | Flights of the cell published as cancelled. | Voos da célula publicados como cancelados. |
| `n_flight_numbers` | int32 | count | recompute | Distinct flight numbers in the cell; not additive, because two groups can reuse a number. | Números de voo distintos na célula; não é aditivo, porque dois grupos podem repetir um número. |
| `dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. |
| `dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `dep_delayed_gt0` | int32 | flights | sum | Realised flights whose departure was more than 0 minutes late. | Voos realizados cuja partida foi com mais de 0 minutos de atraso. |
| `dep_delayed_gt15` | int32 | flights | sum | Realised flights whose departure was more than 15 minutes late. | Voos realizados cuja partida foi com mais de 15 minutos de atraso. |
| `dep_delayed_gt30` | int32 | flights | sum | Realised flights whose departure was more than 30 minutes late. | Voos realizados cuja partida foi com mais de 30 minutos de atraso. |
| `dep_delayed_gt60` | int32 | flights | sum | Realised flights whose departure was more than 60 minutes late. | Voos realizados cuja partida foi com mais de 60 minutos de atraso. |
| `dep_early` | int32 | flights | sum | Realised flights that departured early, signed delay below zero (ADR-0008). | Voos realizados com partida antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `dep_outliers` | int32 | flights | sum | Realised flights whose departure delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de partida atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_dep_delay_min` | float64 | minute | sum | Sum of the signed departure delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de partida com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `sum_dep_delay_pos_min` | float64 | minute | sum | Sum of the departure delays truncated at zero, outliers removed. | Soma dos atrasos de partida truncados em zero, sem outliers. |
| `sum_dep_delay_p15_min` | float64 | minute | sum | Sum of the departure delays counting only what exceeds 15 minutes, zero otherwise. | Soma dos atrasos de partida contando só o que passa de 15 minutos, zero caso contrário. |
| `arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. |
| `arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `arr_delayed_gt0` | int32 | flights | sum | Realised flights whose arrival was more than 0 minutes late. | Voos realizados cuja chegada foi com mais de 0 minutos de atraso. |
| `arr_delayed_gt15` | int32 | flights | sum | Realised flights whose arrival was more than 15 minutes late. | Voos realizados cuja chegada foi com mais de 15 minutos de atraso. |
| `arr_delayed_gt30` | int32 | flights | sum | Realised flights whose arrival was more than 30 minutes late. | Voos realizados cuja chegada foi com mais de 30 minutos de atraso. |
| `arr_delayed_gt60` | int32 | flights | sum | Realised flights whose arrival was more than 60 minutes late. | Voos realizados cuja chegada foi com mais de 60 minutos de atraso. |
| `arr_early` | int32 | flights | sum | Realised flights that arrivaed early, signed delay below zero (ADR-0008). | Voos realizados com chegada antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `arr_outliers` | int32 | flights | sum | Realised flights whose arrival delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de chegada atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_arr_delay_min` | float64 | minute | sum | Sum of the signed arrival delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de chegada com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `sum_arr_delay_pos_min` | float64 | minute | sum | Sum of the arrival delays truncated at zero, outliers removed. | Soma dos atrasos de chegada truncados em zero, sem outliers. |
| `sum_arr_delay_p15_min` | float64 | minute | sum | Sum of the arrival delays counting only what exceeds 15 minutes, zero otherwise. | Soma dos atrasos de chegada contando só o que passa de 15 minutos, zero caso contrário. |
| `arr_delayed_1530` | int32 | flights | sum | Realised flights whose arrival was between 15 and 30 minutes late. | Voos realizados cuja chegada foi com atraso entre 15 e 30 minutos. |
| `recovery_obs` | int32 | flights | sum | Realised flights with both delays observed, the base of the recovery measures. | Voos realizados com os dois atrasos observados, base das medidas de recuperação. |
| `sum_recovery_min` | float64 | minute | sum | Sum of arrival delay minus departure delay; negative means time was made up in the air. | Soma do atraso de chegada menos o de partida; negativo significa recuperação em voo. |
| `recovered_gt15` | int32 | flights | sum | Flights that left more than 15 minutes late and still arrived within 15. | Voos que partiram com mais de 15 minutos de atraso e ainda assim chegaram dentro de 15. |
| `sched_block_obs` | int32 | flights | sum | Flights with a scheduled block time. | Voos com tempo de bloco previsto. |
| `sum_sched_block_min` | float64 | minute | sum | Sum of the scheduled block times. | Soma dos tempos de bloco previstos. |
| `sum_sched_block_sq` | float64 | minute^2 | sum | Sum of the squared scheduled block times, so the standard deviation is exact at any grain. | Soma dos quadrados dos tempos de bloco previstos, para que o desvio-padrão seja exato em qualquer grão. |
| `actual_block_obs` | int32 | flights | sum | Flights with an actual block time. | Voos com tempo de bloco realizado. |
| `sum_actual_block_min` | float64 | minute | sum | Sum of the actual block times. | Soma dos tempos de bloco realizados. |
| `padding_obs` | int32 | flights | sum | Realised flights with both block times, the base of the padding measure. | Voos realizados com os dois tempos de bloco, base da medida de folga. |
| `sum_padding_min` | float64 | minute | sum | Sum of scheduled minus actual block time: positive means the schedule held slack. | Soma do bloco previsto menos o realizado: positivo significa folga na programação. |
| `weekend_flights` | int32 | flights | sum | Flights of the cell on a Saturday or Sunday. | Voos da célula em sábado ou domingo. |
| `night_flights` | int32 | flights | sum | Flights scheduled to depart between 22:00 and 05:59. | Voos com partida prevista entre 22h e 5h59. |
| `cause_none` | int32 | flights | sum | Flights with no justification code: no occurrence was reported. | Voos sem código de justificativa: nenhuma ocorrência foi reportada. |
| `cause_weather` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'weather'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'weather' (meteorologia). |
| `cause_airport_restricted` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'airport_restricted'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'airport_restricted' (aeroporto interditado ou com restrição). |
| `cause_rotation` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'rotation'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'rotation' (rotação de aeronave). |
| `cause_technical` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'technical'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'technical' (técnica (pane, avaria, defeito)). |
| `cause_operational` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'operational'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'operational' (operacional e tráfego aéreo). |
| `cause_authorised` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'authorised'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'authorised' (autorizada). |
| `cause_other` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'other'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'other' (outras). |
| `cause_set_prwheather` | int32 | flights | sum | Flights whose justification code is in the article's 'prwheather' set (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). | Voos cujo código de justificativa está no conjunto 'prwheather' do artigo (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). |
| `cause_set_princident` | int32 | flights | sum | Flights whose justification code is in the article's 'princident' set (DF, DG, HB, MA, TD). | Voos cujo código de justificativa está no conjunto 'princident' do artigo (DF, DG, HB, MA, TD). |
| `cause_set_pr_connc` | int32 | flights | sum | Flights whose justification code is in the article's 'pr_connc' set (RA). | Voos cujo código de justificativa está no conjunto 'pr_connc' do artigo (RA). |
| `cancel_technical` | int32 | flights | sum | Cancelled flights with code XN. | Voos cancelados com código XN. |
| `cancel_weather` | int32 | flights | sum | Cancelled flights with code XO, XT, XS, XI, XJ, XM. | Voos cancelados com código XO, XT, XS, XI, XJ, XM. |
| `cancel_authorised` | int32 | flights | sum | Cancelled flights with code XA, XB. | Voos cancelados com código XA, XB. |
| `sched_dep_h00` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 00 local time. | Voos da célula com partida prevista na hora 00, horário local. |
| `sched_dep_h01` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 01 local time. | Voos da célula com partida prevista na hora 01, horário local. |
| `sched_dep_h02` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 02 local time. | Voos da célula com partida prevista na hora 02, horário local. |
| `sched_dep_h03` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 03 local time. | Voos da célula com partida prevista na hora 03, horário local. |
| `sched_dep_h04` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 04 local time. | Voos da célula com partida prevista na hora 04, horário local. |
| `sched_dep_h05` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 05 local time. | Voos da célula com partida prevista na hora 05, horário local. |
| `sched_dep_h06` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 06 local time. | Voos da célula com partida prevista na hora 06, horário local. |
| `sched_dep_h07` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 07 local time. | Voos da célula com partida prevista na hora 07, horário local. |
| `sched_dep_h08` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 08 local time. | Voos da célula com partida prevista na hora 08, horário local. |
| `sched_dep_h09` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 09 local time. | Voos da célula com partida prevista na hora 09, horário local. |
| `sched_dep_h10` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 10 local time. | Voos da célula com partida prevista na hora 10, horário local. |
| `sched_dep_h11` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 11 local time. | Voos da célula com partida prevista na hora 11, horário local. |
| `sched_dep_h12` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 12 local time. | Voos da célula com partida prevista na hora 12, horário local. |
| `sched_dep_h13` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 13 local time. | Voos da célula com partida prevista na hora 13, horário local. |
| `sched_dep_h14` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 14 local time. | Voos da célula com partida prevista na hora 14, horário local. |
| `sched_dep_h15` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 15 local time. | Voos da célula com partida prevista na hora 15, horário local. |
| `sched_dep_h16` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 16 local time. | Voos da célula com partida prevista na hora 16, horário local. |
| `sched_dep_h17` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 17 local time. | Voos da célula com partida prevista na hora 17, horário local. |
| `sched_dep_h18` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 18 local time. | Voos da célula com partida prevista na hora 18, horário local. |
| `sched_dep_h19` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 19 local time. | Voos da célula com partida prevista na hora 19, horário local. |
| `sched_dep_h20` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 20 local time. | Voos da célula com partida prevista na hora 20, horário local. |
| `sched_dep_h21` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 21 local time. | Voos da célula com partida prevista na hora 21, horário local. |
| `sched_dep_h22` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 22 local time. | Voos da célula com partida prevista na hora 22, horário local. |
| `sched_dep_h23` | int32 | flights | sum | Flights of the cell whose scheduled departure falls in hour 23 local time. | Voos da célula com partida prevista na hora 23, horário local. |
| `is_entry` | int8 | flag | sum | The group operated this route this month and not in the previous calendar month. | O grupo operou esta rota neste mês e não no mês civil anterior. |
| `is_exit` | int8 | flag | sum | The group operated this route this month and not in the next calendar month. | O grupo operou esta rota neste mês e não no mês civil seguinte. |

## City-month (`data/analysis/city_month.parquet`)

91 columns.

| column | type | unit | aggregation | definition (en) | definição (pt) |
|---|---|---|---|---|---|
| `ym` | int32 | YYYYMM | none | Year-month key. | Chave ano-mês. |
| `year` | int16 | year | none | Calendar year. | Ano civil. |
| `month` | int8 | month | none | Calendar month, 1-12. | Mês civil, 1-12. |
| `node` | string | node code | none | City node the row describes (ADR-0001). | Nó de cidade descrito pela linha (ADR-0001). |
| `dep_flights` | int32 | flights | sum | Flights of the universe scheduled to departure at this node. | Voos do universo com partida prevista neste nó. |
| `dep_realized` | int32 | flights | sum | Realised flights scheduled to departure at this node. | Voos realizados com partida prevista neste nó. |
| `dep_cancelled` | int32 | flights | sum | Cancelled flights scheduled to departure at this node. | Voos cancelados com partida prevista neste nó. |
| `dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. |
| `dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `dep_delayed_gt0` | int32 | flights | sum | Realised flights whose departure was more than 0 minutes late. | Voos realizados cuja partida foi com mais de 0 minutos de atraso. |
| `dep_delayed_gt15` | int32 | flights | sum | Realised flights whose departure was more than 15 minutes late. | Voos realizados cuja partida foi com mais de 15 minutos de atraso. |
| `dep_delayed_gt30` | int32 | flights | sum | Realised flights whose departure was more than 30 minutes late. | Voos realizados cuja partida foi com mais de 30 minutos de atraso. |
| `dep_delayed_gt60` | int32 | flights | sum | Realised flights whose departure was more than 60 minutes late. | Voos realizados cuja partida foi com mais de 60 minutos de atraso. |
| `dep_early` | int32 | flights | sum | Realised flights that departured early, signed delay below zero (ADR-0008). | Voos realizados com partida antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `dep_outliers` | int32 | flights | sum | Realised flights whose departure delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de partida atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_dep_delay_min` | float64 | minute | sum | Sum of the signed departure delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de partida com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `sum_dep_delay_pos_min` | float64 | minute | sum | Sum of the departure delays truncated at zero, outliers removed. | Soma dos atrasos de partida truncados em zero, sem outliers. |
| `sum_dep_delay_p15_min` | float64 | minute | sum | Sum of the departure delays counting only what exceeds 15 minutes, zero otherwise. | Soma dos atrasos de partida contando só o que passa de 15 minutos, zero caso contrário. |
| `night_flights` | int32 | flights | sum | Flights scheduled to depart between 22:00 and 05:59. | Voos com partida prevista entre 22h e 5h59. |
| `weekend_flights` | int32 | flights | sum | Flights of the cell on a Saturday or Sunday. | Voos da célula em sábado ou domingo. |
| `cause_none` | int32 | flights | sum | Flights with no justification code: no occurrence was reported. | Voos sem código de justificativa: nenhuma ocorrência foi reportada. |
| `cause_weather` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'weather'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'weather' (meteorologia). |
| `cause_airport_restricted` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'airport_restricted'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'airport_restricted' (aeroporto interditado ou com restrição). |
| `cause_rotation` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'rotation'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'rotation' (rotação de aeronave). |
| `cause_technical` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'technical'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'technical' (técnica (pane, avaria, defeito)). |
| `cause_operational` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'operational'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'operational' (operacional e tráfego aéreo). |
| `cause_authorised` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'authorised'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'authorised' (autorizada). |
| `cause_other` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'other'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'other' (outras). |
| `cause_set_prwheather` | int32 | flights | sum | Flights whose justification code is in the article's 'prwheather' set (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). | Voos cujo código de justificativa está no conjunto 'prwheather' do artigo (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). |
| `cause_set_princident` | int32 | flights | sum | Flights whose justification code is in the article's 'princident' set (DF, DG, HB, MA, TD). | Voos cujo código de justificativa está no conjunto 'princident' do artigo (DF, DG, HB, MA, TD). |
| `cause_set_pr_connc` | int32 | flights | sum | Flights whose justification code is in the article's 'pr_connc' set (RA). | Voos cujo código de justificativa está no conjunto 'pr_connc' do artigo (RA). |
| `cancel_technical` | int32 | flights | sum | Cancelled flights with code XN. | Voos cancelados com código XN. |
| `cancel_weather` | int32 | flights | sum | Cancelled flights with code XO, XT, XS, XI, XJ, XM. | Voos cancelados com código XO, XT, XS, XI, XJ, XM. |
| `cancel_authorised` | int32 | flights | sum | Cancelled flights with code XA, XB. | Voos cancelados com código XA, XB. |
| `arr_flights` | int32 | flights | sum | Flights of the universe scheduled to arrivae at this node. | Voos do universo com chegada prevista neste nó. |
| `arr_realized` | int32 | flights | sum | Realised flights scheduled to arrivae at this node. | Voos realizados com chegada prevista neste nó. |
| `arr_cancelled` | int32 | flights | sum | Cancelled flights scheduled to arrivae at this node. | Voos cancelados com chegada prevista neste nó. |
| `arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. |
| `arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `arr_delayed_gt0` | int32 | flights | sum | Realised flights whose arrival was more than 0 minutes late. | Voos realizados cuja chegada foi com mais de 0 minutos de atraso. |
| `arr_delayed_gt15` | int32 | flights | sum | Realised flights whose arrival was more than 15 minutes late. | Voos realizados cuja chegada foi com mais de 15 minutos de atraso. |
| `arr_delayed_1530` | int32 | flights | sum | Realised flights whose arrival was between 15 and 30 minutes late. | Voos realizados cuja chegada foi com atraso entre 15 e 30 minutos. |
| `arr_delayed_gt30` | int32 | flights | sum | Realised flights whose arrival was more than 30 minutes late. | Voos realizados cuja chegada foi com mais de 30 minutos de atraso. |
| `arr_delayed_gt60` | int32 | flights | sum | Realised flights whose arrival was more than 60 minutes late. | Voos realizados cuja chegada foi com mais de 60 minutos de atraso. |
| `arr_early` | int32 | flights | sum | Realised flights that arrivaed early, signed delay below zero (ADR-0008). | Voos realizados com chegada antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `arr_outliers` | int32 | flights | sum | Realised flights whose arrival delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de chegada atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_arr_delay_min` | float64 | minute | sum | Sum of the signed arrival delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de chegada com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `sum_arr_delay_pos_min` | float64 | minute | sum | Sum of the arrival delays truncated at zero, outliers removed. | Soma dos atrasos de chegada truncados em zero, sem outliers. |
| `sum_arr_delay_p15_min` | float64 | minute | sum | Sum of the arrival delays counting only what exceeds 15 minutes, zero otherwise. | Soma dos atrasos de chegada contando só o que passa de 15 minutos, zero caso contrário. |
| `movements` | int32 | movements | sum | Scheduled movements at the node: departures plus arrivals of the universe. | Movimentos programados no nó: partidas mais chegadas do universo. |
| `movements_realized` | int32 | movements | sum | Movements that operated. | Movimentos que operaram. |
| `movements_cancelled` | int32 | movements | sum | Movements cancelled. | Movimentos cancelados. |
| `n_groups` | int32 | count | recompute | Airline groups with at least one flight in the cell. | Grupos de empresas com ao menos um voo na célula. |
| `hhi_flights` | float32 | index 0-1 | recompute | Herfindahl index over the groups' shares of flights; not the article's passenger-based `rthhi` (ADR-0004). | Índice de Herfindahl sobre as participações dos grupos em voos; não é o `rthhi` do artigo, que usa passageiros (ADR-0004). |
| `sh_leader` | float32 | share | recompute | Flight share of the largest group in the cell. | Participação em voos do maior grupo da célula. |
| `sh_cancel` | float32 | share | recompute | Cancelled flights divided by the flights of the cell. | Voos cancelados divididos pelos voos da célula. |
| `dep_delay_denominator` | float32 | flights | recompute | Flights the departure-delay proportions of this row were divided by, under the convention in force (ADR-0012). | Voos que serviram de denominador às proporções de atraso de partida desta linha, sob a convenção vigente (ADR-0012). |
| `sh_dep_gt0` | float32 | share | recompute | dep_delayed_gt0 divided by the departure-delay denominator. | dep_delayed_gt0 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt15` | float32 | share | recompute | dep_delayed_gt15 divided by the departure-delay denominator. | dep_delayed_gt15 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt30` | float32 | share | recompute | dep_delayed_gt30 divided by the departure-delay denominator. | dep_delayed_gt30 dividido pelo denominador de atraso de partida. |
| `dep_delay_mean_min` | float32 | minute | recompute | Mean signed departure delay over the denominator, outliers removed; early flights count negative (ADR-0008). | Atraso médio de partida com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). |
| `arr_delay_denominator` | float32 | flights | recompute | Flights the arrival-delay proportions of this row were divided by, under the convention in force (ADR-0012). | Voos que serviram de denominador às proporções de atraso de chegada desta linha, sob a convenção vigente (ADR-0012). |
| `sh_arr_gt0` | float32 | share | recompute | arr_delayed_gt0 divided by the arrival-delay denominator. | arr_delayed_gt0 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt15` | float32 | share | recompute | arr_delayed_gt15 divided by the arrival-delay denominator. | arr_delayed_gt15 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt30` | float32 | share | recompute | arr_delayed_gt30 divided by the arrival-delay denominator. | arr_delayed_gt30 dividido pelo denominador de atraso de chegada. |
| `arr_delay_mean_min` | float32 | minute | recompute | Mean signed arrival delay over the denominator, outliers removed; early flights count negative (ADR-0008). | Atraso médio de chegada com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). |
| `sh_cause_weather` | float32 | share | recompute | cause_weather divided by the flights of the cell. | cause_weather dividido pelos voos da célula. |
| `sh_cause_airport_restricted` | float32 | share | recompute | cause_airport_restricted divided by the flights of the cell. | cause_airport_restricted dividido pelos voos da célula. |
| `sh_cause_rotation` | float32 | share | recompute | cause_rotation divided by the flights of the cell. | cause_rotation dividido pelos voos da célula. |
| `sh_cause_technical` | float32 | share | recompute | cause_technical divided by the flights of the cell. | cause_technical dividido pelos voos da célula. |
| `sh_cause_operational` | float32 | share | recompute | cause_operational divided by the flights of the cell. | cause_operational dividido pelos voos da célula. |
| `sh_cause_authorised` | float32 | share | recompute | cause_authorised divided by the flights of the cell. | cause_authorised dividido pelos voos da célula. |
| `sh_cause_other` | float32 | share | recompute | cause_other divided by the flights of the cell. | cause_other dividido pelos voos da célula. |
| `sh_cause_none` | float32 | share | recompute | cause_none divided by the flights of the cell. | cause_none dividido pelos voos da célula. |
| `prwheather` | float32 | share | recompute | Article `prwheather` (the misspelling is the article's): flights coded AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divided by f. Not only weather — its dominant code is AR, restricted airport (ADR-0005). Reproduces at 98.5%. | `prwheather` do artigo (o erro de grafia é do artigo): voos com código AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divididos por f. Não é só clima — seu código dominante é AR, aeroporto com restrições (ADR-0005). Reproduz 98,5%. |
| `princident` | float32 | share | recompute | Article `princident`: flights coded DF, DG, HB, MA, TD divided by f. Reproduces at 99.1%. | `princident` do artigo: voos com código DF, DG, HB, MA, TD divididos por f. Reproduz 99,1%. |
| `pr_connc` | float32 | share | recompute | Article `pr_connc`: flights coded RA divided by f. RA is aircraft rotation, not passengers held for a connection as the article's label says (declared difference). Reproduces at 99.2%. | `pr_connc` do artigo: voos com código RA divididos por f. RA é conexão de aeronave, não espera de passageiros como diz o rótulo do artigo (diferença declarada). Reproduz 99,2%. |
| `peak_hour_share` | float32 | share | recompute | Share of the cell's flights in its busiest scheduled departure hour. | Participação dos voos da célula na hora de partida programada mais carregada. |
| `hhi_hours` | float32 | index 0-1 | recompute | Herfindahl index of the scheduled departure hours: how concentrated the timetable is in the day. | Índice de Herfindahl das horas de partida programadas: quão concentrada é a malha no dia. |
| `sh_movements_fsc` | float32 | share | recompute | Share of the node's movements flown by carriers of class FSC (ADR-0011). | Participação dos movimentos do nó operados por empresas das empresas de serviço completo (FSC) (ADR-0011). |
| `sh_movements_lcc` | float32 | share | recompute | Share of the node's movements flown by carriers of class LCC (ADR-0011). | Participação dos movimentos do nó operados por empresas das empresas de baixo custo (LCC) (ADR-0011). |
| `sh_movements_regional` | float32 | share | recompute | Share of the node's movements flown by carriers of class REGIONAL (ADR-0011). | Participação dos movimentos do nó operados por empresas das regionais (ADR-0011). |
| `sh_movements_other` | float32 | share | recompute | Share of the node's movements flown by carriers of class OTHER (ADR-0011). | Participação dos movimentos do nó operados por empresas das demais empresas (ADR-0011). |
| `lccfu_present` | int8 | flag | recompute | Gol or Azul operated at least one route touching this node this month. | Gol ou Azul operou ao menos uma rota que toca este nó neste mês. |
| `n_hub_groups` | int32 | count | recompute | Groups for which this node is a hub this month (share, ratio and volume floors of vra.hub). | Grupos para os quais este nó é hub neste mês (participação, razão e pisos de volume de vra.hub). |
| `hub_max_score` | float32 | ratio | recompute | Largest hub score at the node: the highest ratio of a group's city share to its system share. | Maior escore de hub no nó: a maior razão entre a participação do grupo na cidade e sua participação no sistema. |
| `mov_hour_max` | float32 | movements | recompute | Busiest single day-hour of the month at this node. | Maior número de movimentos numa dia-hora do mês neste nó. |
| `mov_hour_mean` | float32 | movements | recompute | Mean movements over the day-hours that had any movement. | Média de movimentos nas dia-horas com algum movimento. |
| `congested_hours` | int32 | count | recompute | Day-hours of the month at or above the node's own p90 for the year (ADR-0007). | Dia-horas do mês em ou acima do p90 do próprio nó no ano (ADR-0007). |
| `congested_movements` | int32 | movements | recompute | Movements inside those congested day-hours. | Movimentos dentro dessas dia-horas congestionadas. |
| `sh_movements_congested` | float32 | share | recompute | Share of the node's monthly movements in congested day-hours; the ADR-0007 proxy for the article's `prcongested`. | Participação dos movimentos do mês do nó em dia-horas congestionadas; o proxy do ADR-0007 para o `prcongested` do artigo. |

## Airline x city x month (`data/analysis/airline_city_month.parquet`)

81 columns.

| column | type | unit | aggregation | definition (en) | definição (pt) |
|---|---|---|---|---|---|
| `ym` | int32 | YYYYMM | none | Year-month key. | Chave ano-mês. |
| `year` | int16 | year | none | Calendar year. | Ano civil. |
| `month` | int8 | month | none | Calendar month, 1-12. | Mês civil, 1-12. |
| `node` | string | node code | none | City node the row describes (ADR-0001). | Nó de cidade descrito pela linha (ADR-0001). |
| `group` | string | group code | none | Airline economic group at this month (ADR-0003); an unlabelled airline keeps its own ICAO code. | Grupo econômico da empresa neste mês (ADR-0003); empresa sem rótulo mantém o próprio ICAO. |
| `class` | string | class | none | Business-model class: FSC, LCC, regional or other (ADR-0011). | Classe de modelo de negócio: FSC, LCC, regional ou other (ADR-0011). |
| `dep_flights` | int32 | flights | sum | Flights of the universe scheduled to departure at this node. | Voos do universo com partida prevista neste nó. |
| `dep_realized` | int32 | flights | sum | Realised flights scheduled to departure at this node. | Voos realizados com partida prevista neste nó. |
| `dep_cancelled` | int32 | flights | sum | Cancelled flights scheduled to departure at this node. | Voos cancelados com partida prevista neste nó. |
| `dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. |
| `dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `dep_delayed_gt0` | int32 | flights | sum | Realised flights whose departure was more than 0 minutes late. | Voos realizados cuja partida foi com mais de 0 minutos de atraso. |
| `dep_delayed_gt15` | int32 | flights | sum | Realised flights whose departure was more than 15 minutes late. | Voos realizados cuja partida foi com mais de 15 minutos de atraso. |
| `dep_delayed_gt30` | int32 | flights | sum | Realised flights whose departure was more than 30 minutes late. | Voos realizados cuja partida foi com mais de 30 minutos de atraso. |
| `dep_delayed_gt60` | int32 | flights | sum | Realised flights whose departure was more than 60 minutes late. | Voos realizados cuja partida foi com mais de 60 minutos de atraso. |
| `dep_early` | int32 | flights | sum | Realised flights that departured early, signed delay below zero (ADR-0008). | Voos realizados com partida antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `dep_outliers` | int32 | flights | sum | Realised flights whose departure delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de partida atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_dep_delay_min` | float64 | minute | sum | Sum of the signed departure delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de partida com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `sum_dep_delay_pos_min` | float64 | minute | sum | Sum of the departure delays truncated at zero, outliers removed. | Soma dos atrasos de partida truncados em zero, sem outliers. |
| `sum_dep_delay_p15_min` | float64 | minute | sum | Sum of the departure delays counting only what exceeds 15 minutes, zero otherwise. | Soma dos atrasos de partida contando só o que passa de 15 minutos, zero caso contrário. |
| `night_flights` | int32 | flights | sum | Flights scheduled to depart between 22:00 and 05:59. | Voos com partida prevista entre 22h e 5h59. |
| `weekend_flights` | int32 | flights | sum | Flights of the cell on a Saturday or Sunday. | Voos da célula em sábado ou domingo. |
| `cause_none` | int32 | flights | sum | Flights with no justification code: no occurrence was reported. | Voos sem código de justificativa: nenhuma ocorrência foi reportada. |
| `cause_weather` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'weather'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'weather' (meteorologia). |
| `cause_airport_restricted` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'airport_restricted'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'airport_restricted' (aeroporto interditado ou com restrição). |
| `cause_rotation` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'rotation'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'rotation' (rotação de aeronave). |
| `cause_technical` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'technical'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'technical' (técnica (pane, avaria, defeito)). |
| `cause_operational` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'operational'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'operational' (operacional e tráfego aéreo). |
| `cause_authorised` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'authorised'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'authorised' (autorizada). |
| `cause_other` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'other'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'other' (outras). |
| `cause_set_prwheather` | int32 | flights | sum | Flights whose justification code is in the article's 'prwheather' set (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). | Voos cujo código de justificativa está no conjunto 'prwheather' do artigo (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). |
| `cause_set_princident` | int32 | flights | sum | Flights whose justification code is in the article's 'princident' set (DF, DG, HB, MA, TD). | Voos cujo código de justificativa está no conjunto 'princident' do artigo (DF, DG, HB, MA, TD). |
| `cause_set_pr_connc` | int32 | flights | sum | Flights whose justification code is in the article's 'pr_connc' set (RA). | Voos cujo código de justificativa está no conjunto 'pr_connc' do artigo (RA). |
| `cancel_technical` | int32 | flights | sum | Cancelled flights with code XN. | Voos cancelados com código XN. |
| `cancel_weather` | int32 | flights | sum | Cancelled flights with code XO, XT, XS, XI, XJ, XM. | Voos cancelados com código XO, XT, XS, XI, XJ, XM. |
| `cancel_authorised` | int32 | flights | sum | Cancelled flights with code XA, XB. | Voos cancelados com código XA, XB. |
| `arr_flights` | int32 | flights | sum | Flights of the universe scheduled to arrivae at this node. | Voos do universo com chegada prevista neste nó. |
| `arr_realized` | int32 | flights | sum | Realised flights scheduled to arrivae at this node. | Voos realizados com chegada prevista neste nó. |
| `arr_cancelled` | int32 | flights | sum | Cancelled flights scheduled to arrivae at this node. | Voos cancelados com chegada prevista neste nó. |
| `arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. |
| `arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `arr_delayed_gt0` | int32 | flights | sum | Realised flights whose arrival was more than 0 minutes late. | Voos realizados cuja chegada foi com mais de 0 minutos de atraso. |
| `arr_delayed_gt15` | int32 | flights | sum | Realised flights whose arrival was more than 15 minutes late. | Voos realizados cuja chegada foi com mais de 15 minutos de atraso. |
| `arr_delayed_1530` | int32 | flights | sum | Realised flights whose arrival was between 15 and 30 minutes late. | Voos realizados cuja chegada foi com atraso entre 15 e 30 minutos. |
| `arr_delayed_gt30` | int32 | flights | sum | Realised flights whose arrival was more than 30 minutes late. | Voos realizados cuja chegada foi com mais de 30 minutos de atraso. |
| `arr_delayed_gt60` | int32 | flights | sum | Realised flights whose arrival was more than 60 minutes late. | Voos realizados cuja chegada foi com mais de 60 minutos de atraso. |
| `arr_early` | int32 | flights | sum | Realised flights that arrivaed early, signed delay below zero (ADR-0008). | Voos realizados com chegada antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `arr_outliers` | int32 | flights | sum | Realised flights whose arrival delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de chegada atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_arr_delay_min` | float64 | minute | sum | Sum of the signed arrival delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de chegada com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `sum_arr_delay_pos_min` | float64 | minute | sum | Sum of the arrival delays truncated at zero, outliers removed. | Soma dos atrasos de chegada truncados em zero, sem outliers. |
| `sum_arr_delay_p15_min` | float64 | minute | sum | Sum of the arrival delays counting only what exceeds 15 minutes, zero otherwise. | Soma dos atrasos de chegada contando só o que passa de 15 minutos, zero caso contrário. |
| `movements` | int32 | movements | sum | Scheduled movements at the node: departures plus arrivals of the universe. | Movimentos programados no nó: partidas mais chegadas do universo. |
| `movements_realized` | int32 | movements | sum | Movements that operated. | Movimentos que operaram. |
| `movements_cancelled` | int32 | movements | sum | Movements cancelled. | Movimentos cancelados. |
| `sh_cancel` | float32 | share | recompute | Cancelled flights divided by the flights of the cell. | Voos cancelados divididos pelos voos da célula. |
| `dep_delay_denominator` | float32 | flights | recompute | Flights the departure-delay proportions of this row were divided by, under the convention in force (ADR-0012). | Voos que serviram de denominador às proporções de atraso de partida desta linha, sob a convenção vigente (ADR-0012). |
| `sh_dep_gt0` | float32 | share | recompute | dep_delayed_gt0 divided by the departure-delay denominator. | dep_delayed_gt0 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt15` | float32 | share | recompute | dep_delayed_gt15 divided by the departure-delay denominator. | dep_delayed_gt15 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt30` | float32 | share | recompute | dep_delayed_gt30 divided by the departure-delay denominator. | dep_delayed_gt30 dividido pelo denominador de atraso de partida. |
| `dep_delay_mean_min` | float32 | minute | recompute | Mean signed departure delay over the denominator, outliers removed; early flights count negative (ADR-0008). | Atraso médio de partida com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). |
| `arr_delay_denominator` | float32 | flights | recompute | Flights the arrival-delay proportions of this row were divided by, under the convention in force (ADR-0012). | Voos que serviram de denominador às proporções de atraso de chegada desta linha, sob a convenção vigente (ADR-0012). |
| `sh_arr_gt0` | float32 | share | recompute | arr_delayed_gt0 divided by the arrival-delay denominator. | arr_delayed_gt0 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt15` | float32 | share | recompute | arr_delayed_gt15 divided by the arrival-delay denominator. | arr_delayed_gt15 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt30` | float32 | share | recompute | arr_delayed_gt30 divided by the arrival-delay denominator. | arr_delayed_gt30 dividido pelo denominador de atraso de chegada. |
| `arr_delay_mean_min` | float32 | minute | recompute | Mean signed arrival delay over the denominator, outliers removed; early flights count negative (ADR-0008). | Atraso médio de chegada com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). |
| `sh_cause_weather` | float32 | share | recompute | cause_weather divided by the flights of the cell. | cause_weather dividido pelos voos da célula. |
| `sh_cause_airport_restricted` | float32 | share | recompute | cause_airport_restricted divided by the flights of the cell. | cause_airport_restricted dividido pelos voos da célula. |
| `sh_cause_rotation` | float32 | share | recompute | cause_rotation divided by the flights of the cell. | cause_rotation dividido pelos voos da célula. |
| `sh_cause_technical` | float32 | share | recompute | cause_technical divided by the flights of the cell. | cause_technical dividido pelos voos da célula. |
| `sh_cause_operational` | float32 | share | recompute | cause_operational divided by the flights of the cell. | cause_operational dividido pelos voos da célula. |
| `sh_cause_authorised` | float32 | share | recompute | cause_authorised divided by the flights of the cell. | cause_authorised dividido pelos voos da célula. |
| `sh_cause_other` | float32 | share | recompute | cause_other divided by the flights of the cell. | cause_other dividido pelos voos da célula. |
| `sh_cause_none` | float32 | share | recompute | cause_none divided by the flights of the cell. | cause_none dividido pelos voos da célula. |
| `prwheather` | float32 | share | recompute | Article `prwheather` (the misspelling is the article's): flights coded AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divided by f. Not only weather — its dominant code is AR, restricted airport (ADR-0005). Reproduces at 98.5%. | `prwheather` do artigo (o erro de grafia é do artigo): voos com código AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divididos por f. Não é só clima — seu código dominante é AR, aeroporto com restrições (ADR-0005). Reproduz 98,5%. |
| `princident` | float32 | share | recompute | Article `princident`: flights coded DF, DG, HB, MA, TD divided by f. Reproduces at 99.1%. | `princident` do artigo: voos com código DF, DG, HB, MA, TD divididos por f. Reproduz 99,1%. |
| `pr_connc` | float32 | share | recompute | Article `pr_connc`: flights coded RA divided by f. RA is aircraft rotation, not passengers held for a connection as the article's label says (declared difference). Reproduces at 99.2%. | `pr_connc` do artigo: voos com código RA divididos por f. RA é conexão de aeronave, não espera de passageiros como diz o rótulo do artigo (diferença declarada). Reproduz 99,2%. |
| `peak_hour_share` | float32 | share | recompute | Share of the cell's flights in its busiest scheduled departure hour. | Participação dos voos da célula na hora de partida programada mais carregada. |
| `hhi_hours` | float32 | index 0-1 | recompute | Herfindahl index of the scheduled departure hours: how concentrated the timetable is in the day. | Índice de Herfindahl das horas de partida programadas: quão concentrada é a malha no dia. |
| `city_share` | float32 | share | recompute | The group's share of this node's movements in the month. | Participação do grupo nos movimentos deste nó no mês. |
| `hub_score` | float32 | ratio | recompute | The group's city share divided by its share of all movements in the same month. | Participação do grupo na cidade dividida por sua participação em todos os movimentos do mesmo mês. |
| `is_hub` | int8 | flag | recompute | The node is a hub for this group: share at least 0.20, ratio at least 2, above both volume floors. | O nó é hub deste grupo: participação de ao menos 0,20, razão de ao menos 2, acima dos dois pisos de volume. |

## Route-month panel (`data/analysis/panel_route_month.parquet`)

228 columns.

| column | type | unit | aggregation | definition (en) | definição (pt) |
|---|---|---|---|---|---|
| `route` | string | node pair | none | Directional route, origin_node-dest_node (ADR-0001). | Rota direcional, origin_node-dest_node (ADR-0001). |
| `ym` | int32 | YYYYMM | none | Year-month key. | Chave ano-mês. |
| `year` | int16 | year | none | Calendar year. | Ano civil. |
| `month` | int8 | month | none | Calendar month, 1-12. | Mês civil, 1-12. |
| `origin_node` | string | node code | none | Origin node (ADR-0001). | Nó de origem (ADR-0001). |
| `dest_node` | string | node code | none | Destination node (ADR-0001). | Nó de destino (ADR-0001). |
| `ndays` | int16 | days | none | Days in the calendar month. | Dias do mês civil. |
| `distance_km` | float32 | kilometre | none | Great-circle distance between the two nodes, from data/external/distances_km.csv. | Distância great-circle entre os dois nós, de data/external/distances_km.csv. |
| `flights` | int32 | flights | sum | Flights of the replication universe scheduled in the cell: realised plus cancelled (ADR-0002). This is the article's `f`. | Voos do universo de replicação programados na célula: realizados mais cancelados (ADR-0002). É o `f` do artigo. |
| `realized` | int32 | flights | sum | Flights of the cell that operated. | Voos da célula que operaram. |
| `cancelled` | int32 | flights | sum | Flights of the cell published as cancelled. | Voos da célula publicados como cancelados. |
| `dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. |
| `dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `dep_delayed_gt0` | int32 | flights | sum | Realised flights whose departure was more than 0 minutes late. | Voos realizados cuja partida foi com mais de 0 minutos de atraso. |
| `dep_delayed_gt15` | int32 | flights | sum | Realised flights whose departure was more than 15 minutes late. | Voos realizados cuja partida foi com mais de 15 minutos de atraso. |
| `dep_delayed_gt30` | int32 | flights | sum | Realised flights whose departure was more than 30 minutes late. | Voos realizados cuja partida foi com mais de 30 minutos de atraso. |
| `dep_delayed_gt60` | int32 | flights | sum | Realised flights whose departure was more than 60 minutes late. | Voos realizados cuja partida foi com mais de 60 minutos de atraso. |
| `dep_early` | int32 | flights | sum | Realised flights that departured early, signed delay below zero (ADR-0008). | Voos realizados com partida antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `dep_outliers` | int32 | flights | sum | Realised flights whose departure delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de partida atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_dep_delay_min` | float64 | minute | sum | Sum of the signed departure delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de partida com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. |
| `arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). |
| `arr_delayed_gt0` | int32 | flights | sum | Realised flights whose arrival was more than 0 minutes late. | Voos realizados cuja chegada foi com mais de 0 minutos de atraso. |
| `arr_delayed_gt15` | int32 | flights | sum | Realised flights whose arrival was more than 15 minutes late. | Voos realizados cuja chegada foi com mais de 15 minutos de atraso. |
| `arr_delayed_gt30` | int32 | flights | sum | Realised flights whose arrival was more than 30 minutes late. | Voos realizados cuja chegada foi com mais de 30 minutos de atraso. |
| `arr_delayed_gt60` | int32 | flights | sum | Realised flights whose arrival was more than 60 minutes late. | Voos realizados cuja chegada foi com mais de 60 minutos de atraso. |
| `arr_early` | int32 | flights | sum | Realised flights that arrivaed early, signed delay below zero (ADR-0008). | Voos realizados com chegada antecipada, atraso com sinal abaixo de zero (ADR-0008). |
| `arr_outliers` | int32 | flights | sum | Realised flights whose arrival delay reaches the outlier threshold of 313.25 minutes (ADR-0008); excluded from every sum of minutes. | Voos realizados cujo atraso de chegada atinge o limiar de outlier de 313,25 minutos (ADR-0008); excluídos de toda soma de minutos. |
| `sum_arr_delay_min` | float64 | minute | sum | Sum of the signed arrival delays, outliers removed, early flights negative (ADR-0008). | Soma dos atrasos de chegada com sinal, sem outliers, antecipações negativas (ADR-0008). |
| `arr_delayed_1530` | int32 | flights | sum | Realised flights whose arrival was between 15 and 30 minutes late. | Voos realizados cuja chegada foi com atraso entre 15 e 30 minutos. |
| `recovery_obs` | int32 | flights | sum | Realised flights with both delays observed, the base of the recovery measures. | Voos realizados com os dois atrasos observados, base das medidas de recuperação. |
| `recovered_gt15` | int32 | flights | sum | Flights that left more than 15 minutes late and still arrived within 15. | Voos que partiram com mais de 15 minutos de atraso e ainda assim chegaram dentro de 15. |
| `sched_block_obs` | int32 | flights | sum | Flights with a scheduled block time. | Voos com tempo de bloco previsto. |
| `actual_block_obs` | int32 | flights | sum | Flights with an actual block time. | Voos com tempo de bloco realizado. |
| `padding_obs` | int32 | flights | sum | Realised flights with both block times, the base of the padding measure. | Voos realizados com os dois tempos de bloco, base da medida de folga. |
| `weekend_flights` | int32 | flights | sum | Flights of the cell on a Saturday or Sunday. | Voos da célula em sábado ou domingo. |
| `night_flights` | int32 | flights | sum | Flights scheduled to depart between 22:00 and 05:59. | Voos com partida prevista entre 22h e 5h59. |
| `cause_none` | int32 | flights | sum | Flights with no justification code: no occurrence was reported. | Voos sem código de justificativa: nenhuma ocorrência foi reportada. |
| `cause_weather` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'weather'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'weather' (meteorologia). |
| `cause_airport_restricted` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'airport_restricted'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'airport_restricted' (aeroporto interditado ou com restrição). |
| `cause_rotation` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'rotation'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'rotation' (rotação de aeronave). |
| `cause_technical` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'technical'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'technical' (técnica (pane, avaria, defeito)). |
| `cause_operational` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'operational'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'operational' (operacional e tráfego aéreo). |
| `cause_authorised` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'authorised'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'authorised' (autorizada). |
| `cause_other` | int32 | flights | sum | Flights whose IAC 1504 justification code falls in the ADR-0005 category 'other'. | Voos cujo código de justificativa da IAC 1504 pertence à categoria ADR-0005 'other' (outras). |
| `cause_set_prwheather` | int32 | flights | sum | Flights whose justification code is in the article's 'prwheather' set (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). | Voos cujo código de justificativa está no conjunto 'prwheather' do artigo (AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT). |
| `cause_set_princident` | int32 | flights | sum | Flights whose justification code is in the article's 'princident' set (DF, DG, HB, MA, TD). | Voos cujo código de justificativa está no conjunto 'princident' do artigo (DF, DG, HB, MA, TD). |
| `cause_set_pr_connc` | int32 | flights | sum | Flights whose justification code is in the article's 'pr_connc' set (RA). | Voos cujo código de justificativa está no conjunto 'pr_connc' do artigo (RA). |
| `cancel_technical` | int32 | flights | sum | Cancelled flights with code XN. | Voos cancelados com código XN. |
| `cancel_weather` | int32 | flights | sum | Cancelled flights with code XO, XT, XS, XI, XJ, XM. | Voos cancelados com código XO, XT, XS, XI, XJ, XM. |
| `cancel_authorised` | int32 | flights | sum | Cancelled flights with code XA, XB. | Voos cancelados com código XA, XB. |
| `n_groups` | int32 | count | recompute | Airline groups with at least one flight in the cell. | Grupos de empresas com ao menos um voo na célula. |
| `hhi_flights` | float32 | index 0-1 | recompute | Herfindahl index over the groups' shares of flights; not the article's passenger-based `rthhi` (ADR-0004). | Índice de Herfindahl sobre as participações dos grupos em voos; não é o `rthhi` do artigo, que usa passageiros (ADR-0004). |
| `sh_leader` | float32 | share | recompute | Flight share of the largest group in the cell. | Participação em voos do maior grupo da célula. |
| `n_entries` | int32 | count | sum | Groups operating the route this month that did not operate it in the previous calendar month. | Grupos operando a rota neste mês que não a operavam no mês civil anterior. |
| `n_exits` | int32 | count | sum | Groups operating the route this month that do not operate it in the next calendar month. | Grupos operando a rota neste mês que não a operam no mês civil seguinte. |
| `sh_flights_fsc` | float32 | share | recompute | Share of the cell's flights flown by carriers of class FSC (ADR-0011). | Participação dos voos da célula operados por empresas das empresas de serviço completo (FSC) (ADR-0011). |
| `sh_flights_lcc` | float32 | share | recompute | Share of the cell's flights flown by carriers of class LCC (ADR-0011). | Participação dos voos da célula operados por empresas das empresas de baixo custo (LCC) (ADR-0011). |
| `sh_flights_regional` | float32 | share | recompute | Share of the cell's flights flown by carriers of class REGIONAL (ADR-0011). | Participação dos voos da célula operados por empresas das regionais (ADR-0011). |
| `sh_flights_other` | float32 | share | recompute | Share of the cell's flights flown by carriers of class OTHER (ADR-0011). | Participação dos voos da célula operados por empresas das demais empresas (ADR-0011). |
| `entry_lcc` | int8 | flag | recompute | Gol or Azul entered this route this month. | Gol ou Azul entrou nesta rota neste mês. |
| `sh_cancel` | float32 | share | recompute | Cancelled flights divided by the flights of the cell. | Voos cancelados divididos pelos voos da célula. |
| `dep_delay_denominator` | float32 | flights | recompute | Flights the departure-delay proportions of this row were divided by, under the convention in force (ADR-0012). | Voos que serviram de denominador às proporções de atraso de partida desta linha, sob a convenção vigente (ADR-0012). |
| `sh_dep_gt0` | float32 | share | recompute | dep_delayed_gt0 divided by the departure-delay denominator. | dep_delayed_gt0 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt15` | float32 | share | recompute | dep_delayed_gt15 divided by the departure-delay denominator. | dep_delayed_gt15 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt30` | float32 | share | recompute | dep_delayed_gt30 divided by the departure-delay denominator. | dep_delayed_gt30 dividido pelo denominador de atraso de partida. |
| `sh_dep_gt60` | float32 | share | recompute | dep_delayed_gt60 divided by the departure-delay denominator. | dep_delayed_gt60 dividido pelo denominador de atraso de partida. |
| `sh_dep_early` | float32 | share | recompute | dep_early divided by the departure-delay denominator. | dep_early dividido pelo denominador de atraso de partida. |
| `dep_delay_mean_min` | float32 | minute | recompute | Mean signed departure delay over the denominator, outliers removed; early flights count negative (ADR-0008). | Atraso médio de partida com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). |
| `dep_delay_mean_pos_min` | float32 | minute | recompute | Mean departure delay truncated at zero, the private vintage's convention (ADR-0008). | Atraso médio de partida truncado em zero, a convenção da safra privada (ADR-0008). |
| `arr_delay_denominator` | float32 | flights | recompute | Flights the arrival-delay proportions of this row were divided by, under the convention in force (ADR-0012). | Voos que serviram de denominador às proporções de atraso de chegada desta linha, sob a convenção vigente (ADR-0012). |
| `sh_arr_gt0` | float32 | share | recompute | arr_delayed_gt0 divided by the arrival-delay denominator. | arr_delayed_gt0 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt15` | float32 | share | recompute | arr_delayed_gt15 divided by the arrival-delay denominator. | arr_delayed_gt15 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt30` | float32 | share | recompute | arr_delayed_gt30 divided by the arrival-delay denominator. | arr_delayed_gt30 dividido pelo denominador de atraso de chegada. |
| `sh_arr_gt60` | float32 | share | recompute | arr_delayed_gt60 divided by the arrival-delay denominator. | arr_delayed_gt60 dividido pelo denominador de atraso de chegada. |
| `sh_arr_early` | float32 | share | recompute | arr_early divided by the arrival-delay denominator. | arr_early dividido pelo denominador de atraso de chegada. |
| `arr_delay_mean_min` | float32 | minute | recompute | Mean signed arrival delay over the denominator, outliers removed; early flights count negative (ADR-0008). | Atraso médio de chegada com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). |
| `arr_delay_mean_pos_min` | float32 | minute | recompute | Mean arrival delay truncated at zero, the private vintage's convention (ADR-0008). | Atraso médio de chegada truncado em zero, a convenção da safra privada (ADR-0008). |
| `sh_arr_1530` | float32 | share | recompute | arr_delayed_1530 divided by the arrival-delay denominator. | arr_delayed_1530 dividido pelo denominador de atraso de chegada. |
| `sched_block_mean_min` | float32 | minute | recompute | Mean scheduled block time of the cell. | Tempo de bloco previsto médio da célula. |
| `sched_block_sd_min` | float32 | minute | recompute | Sample standard deviation of the scheduled block time, from the two sums. | Desvio-padrão amostral do tempo de bloco previsto, calculado das duas somas. |
| `padding_mean_min` | float32 | minute | recompute | Mean scheduled slack: scheduled minus actual block time. | Folga média: bloco previsto menos realizado. |
| `recovery_mean_min` | float32 | minute | recompute | Mean in-flight recovery: arrival delay minus departure delay. | Recuperação média em voo: atraso de chegada menos o de partida. |
| `sh_recovered` | float32 | share | recompute | recovered_gt15 divided by recovery_obs. | recovered_gt15 dividido por recovery_obs. |
| `sh_night` | float32 | share | recompute | night_flights divided by the flights of the cell. | night_flights dividido pelos voos da célula. |
| `sh_weekend` | float32 | share | recompute | weekend_flights divided by the flights of the cell. | weekend_flights dividido pelos voos da célula. |
| `peak_hour_share` | float32 | share | recompute | Share of the cell's flights in its busiest scheduled departure hour. | Participação dos voos da célula na hora de partida programada mais carregada. |
| `hhi_hours` | float32 | index 0-1 | recompute | Herfindahl index of the scheduled departure hours: how concentrated the timetable is in the day. | Índice de Herfindahl das horas de partida programadas: quão concentrada é a malha no dia. |
| `sh_cause_weather` | float32 | share | recompute | cause_weather divided by the flights of the cell. | cause_weather dividido pelos voos da célula. |
| `sh_cause_airport_restricted` | float32 | share | recompute | cause_airport_restricted divided by the flights of the cell. | cause_airport_restricted dividido pelos voos da célula. |
| `sh_cause_rotation` | float32 | share | recompute | cause_rotation divided by the flights of the cell. | cause_rotation dividido pelos voos da célula. |
| `sh_cause_technical` | float32 | share | recompute | cause_technical divided by the flights of the cell. | cause_technical dividido pelos voos da célula. |
| `sh_cause_operational` | float32 | share | recompute | cause_operational divided by the flights of the cell. | cause_operational dividido pelos voos da célula. |
| `sh_cause_authorised` | float32 | share | recompute | cause_authorised divided by the flights of the cell. | cause_authorised dividido pelos voos da célula. |
| `sh_cause_other` | float32 | share | recompute | cause_other divided by the flights of the cell. | cause_other dividido pelos voos da célula. |
| `sh_cause_none` | float32 | share | recompute | cause_none divided by the flights of the cell. | cause_none dividido pelos voos da célula. |
| `prwheather` | float32 | share | recompute | Article `prwheather` (the misspelling is the article's): flights coded AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divided by f. Not only weather — its dominant code is AR, restricted airport (ADR-0005). Reproduces at 98.5%. | `prwheather` do artigo (o erro de grafia é do artigo): voos com código AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divididos por f. Não é só clima — seu código dominante é AR, aeroporto com restrições (ADR-0005). Reproduz 98,5%. |
| `princident` | float32 | share | recompute | Article `princident`: flights coded DF, DG, HB, MA, TD divided by f. Reproduces at 99.1%. | `princident` do artigo: voos com código DF, DG, HB, MA, TD divididos por f. Reproduz 99,1%. |
| `pr_connc` | float32 | share | recompute | Article `pr_connc`: flights coded RA divided by f. RA is aircraft rotation, not passengers held for a connection as the article's label says (declared difference). Reproduces at 99.2%. | `pr_connc` do artigo: voos com código RA divididos por f. RA é conexão de aeronave, não espera de passageiros como diz o rótulo do artigo (diferença declarada). Reproduz 99,2%. |
| `sh_cancel_technical` | float32 | share | recompute | cancel_technical divided by the cancelled flights of the cell. | cancel_technical dividido pelos voos cancelados da célula. |
| `sh_cancel_weather` | float32 | share | recompute | cancel_weather divided by the cancelled flights of the cell. | cancel_weather dividido pelos voos cancelados da célula. |
| `sh_cancel_authorised` | float32 | share | recompute | cancel_authorised divided by the cancelled flights of the cell. | cancel_authorised dividido pelos voos cancelados da célula. |
| `fsc_dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. Restricted to the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. Restrito a o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `fsc_dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `fsc_arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. Restricted to the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. Restrito a o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `fsc_arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `lccclass_dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. Restricted to carriers of class LCC, Webjet included. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. Restrito a empresas de classe LCC, Webjet incluída. |
| `lccclass_dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to carriers of class LCC, Webjet included. | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a empresas de classe LCC, Webjet incluída. |
| `lccclass_arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. Restricted to carriers of class LCC, Webjet included. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. Restrito a empresas de classe LCC, Webjet incluída. |
| `lccclass_arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to carriers of class LCC, Webjet included. | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a empresas de classe LCC, Webjet incluída. |
| `fscc_dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. Restricted to carriers of class FSC (ADR-0003). | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. Restrito a empresas de classe FSC (ADR-0003). |
| `fscc_dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to carriers of class FSC (ADR-0003). | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a empresas de classe FSC (ADR-0003). |
| `fscc_arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. Restricted to carriers of class FSC (ADR-0003). | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. Restrito a empresas de classe FSC (ADR-0003). |
| `fscc_arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to carriers of class FSC (ADR-0003). | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a empresas de classe FSC (ADR-0003). |
| `lccfu_dep_delay_obs` | int32 | flights | sum | Realised flights with an observed signed departure delay, both timestamps present. Restricted to the Gol and Azul groups, the article's LCC set. | Voos realizados com atraso de partida observado e com sinal, os dois horários presentes. Restrito a os grupos Gol e Azul, o conjunto LCC do artigo. |
| `lccfu_dep_missing_actual` | int32 | flights | sum | Realised flights with a scheduled departure but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to the Gol and Azul groups, the article's LCC set. | Voos realizados com partida prevista e sem partida real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a os grupos Gol e Azul, o conjunto LCC do artigo. |
| `lccfu_arr_delay_obs` | int32 | flights | sum | Realised flights with an observed signed arrival delay, both timestamps present. Restricted to the Gol and Azul groups, the article's LCC set. | Voos realizados com atraso de chegada observado e com sinal, os dois horários presentes. Restrito a os grupos Gol e Azul, o conjunto LCC do artigo. |
| `lccfu_arr_missing_actual` | int32 | flights | sum | Realised flights with a scheduled arrival but no actual one — the rows the 2019 vintage read as on time (ADR-0012). Restricted to the Gol and Azul groups, the article's LCC set. | Voos realizados com chegada prevista e sem chegada real — as linhas que a safra de 2019 leu como pontuais (ADR-0012). Restrito a os grupos Gol e Azul, o conjunto LCC do artigo. |
| `f` | int32 | flights | sum | Article `f`: flights scheduled on the route-month, realised plus cancelled (ADR-0002). Reproduces the benchmark on 97.5% of route-months. | `f` do artigo: voos programados na rota-mês, realizados mais cancelados (ADR-0002). Reproduz o gabarito em 97,5% das rota-meses. |
| `fl_real` | int32 | flights | sum | Flights of the route-month that operated. | Voos da rota-mês que operaram. |
| `fl_can` | int32 | flights | sum | Article `fl_can`: flights of the route-month that did not operate. | `fl_can` do artigo: voos da rota-mês que não operaram. |
| `fl_odel` | int32 | flights | sum | Article `fl_odel`: realised flights that departed more than 0 minutes late — the benchmark's cut is 0, not 15. | `fl_odel` do artigo: voos realizados que partiram com mais de 0 minuto de atraso — o corte do gabarito é 0, não 15. |
| `fl_ddel` | int32 | flights | sum | Article `fl_ddel`: realised flights that arrived more than 0 minutes late. Reproduces at 59.8% under the same rule that gives 92.4% for departures; the asymmetry is declared, not resolved (ADR-0002). | `fl_ddel` do artigo: voos realizados que chegaram com mais de 0 minuto de atraso. Reproduz 59,8% sob a mesma regra que dá 92,4% nas partidas; a assimetria está declarada, não resolvida (ADR-0002). |
| `dailyfl` | float32 | flights/day | recompute | Article `dailyfl`: f divided by the days in the month. | `dailyfl` do artigo: f dividido pelos dias do mês. |
| `prcanc` | float32 | share | recompute | Article `prcanc`: fl_can divided by f. | `prcanc` do artigo: fl_can dividido por f. |
| `dailyfl00` | float32 | hundred flights/day | recompute | Article `dailyfl00`: dailyfl divided by 100. | `dailyfl00` do artigo: dailyfl dividido por 100. |
| `fsc_f` | int32 | flights | sum | Flights of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil on the route-month, realised plus cancelled. | Voos o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil na rota-mês, realizados mais cancelados. |
| `fsc_n` | int32 | flights | sum | Realised flights of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil on the route-month. | Voos realizados o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil na rota-mês. |
| `fsc_prdelarr` | float32 | share | recompute | Flights of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil arriving more than 15 minutes late, over their arrival denominator. The article's ODDS is built on this. | Voos o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil chegando com mais de 15 minutos de atraso, sobre o denominador de chegada. O ODDS do artigo é construído sobre isto. |
| `fsc_prdelarr1530` | float32 | share | recompute | Flights of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil arriving between 15 and 30 minutes late. | Voos o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil chegando com atraso entre 15 e 30 minutos. |
| `fsc_prdelarr30m` | float32 | share | recompute | Flights of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil arriving more than 30 minutes late. | Voos o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil chegando com mais de 30 minutos de atraso. |
| `fsc_prdeldep` | float32 | share | recompute | Flights of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil departing more than 15 minutes late. | Voos o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil partindo com mais de 15 minutos de atraso. |
| `fsc_oddsarr` | float32 | log-odds | recompute | Log-odds of the arrival proportion of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil; null at 0 and 1. | Log-odds da proporção de chegada o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil; nulo em 0 e 1. |
| `fsc_oddsdep` | float32 | log-odds | recompute | Log-odds of the departure proportion of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | Log-odds da proporção de partida o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `fsc_minsarr` | float32 | minute/flight | recompute | Sum of the signed arrival delays of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil divided by the realised flights of ALL carriers on the route-month. The denominator is the article's, and it dilutes the mean by the other carriers' share (declared difference). | Soma dos atrasos de chegada com sinal o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil dividida pelos voos realizados de TODAS as empresas na rota-mês. O denominador é o do artigo, e dilui a média pela participação das outras empresas (diferença declarada). |
| `fsc_minsdep` | float32 | minute/flight | recompute | The departure counterpart of minsarr for the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | A contrapartida de partida de minsarr o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `fsc_minsp15arr` | float32 | minute/flight | recompute | Sum of the arrival delays of the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil counting only what exceeds 15 minutes, over the same denominator. | Soma dos atrasos de chegada o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil contando só o que passa de 15 minutos, sobre o mesmo denominador. |
| `fsc_minsp15dep` | float32 | minute/flight | recompute | The departure counterpart of minsp15arr for the article's FSC group set (TAM, Varig, Transbrasil, Vasp), which excludes Avianca Brasil. | A contrapartida de partida de minsp15arr o conjunto FSC do artigo (TAM, Varig, Transbrasil, Vasp), que exclui a Avianca Brasil. |
| `fsc_minsarr_trunc` | float32 | minute/flight | recompute | minsarr with early arrivals truncated at zero, the private vintage's convention (ADR-0008); published next to the signed one, not instead of it. | minsarr com chegadas antecipadas truncadas em zero, a convenção da safra privada (ADR-0008); publicado ao lado da versão com sinal, não no lugar dela. |
| `fsc_minsdep_trunc` | float32 | minute/flight | recompute | The departure counterpart of minsarr_trunc. | A contrapartida de partida de minsarr_trunc. |
| `lccclass_n` | int32 | flights | sum | Realised flights of carriers of class LCC, Webjet included on the route-month. | Voos realizados empresas de classe LCC, Webjet incluída na rota-mês. |
| `lccclass_prdelarr` | float32 | share | recompute | Flights of carriers of class LCC, Webjet included arriving more than 15 minutes late, over their arrival denominator. The article's ODDS is built on this. | Voos empresas de classe LCC, Webjet incluída chegando com mais de 15 minutos de atraso, sobre o denominador de chegada. O ODDS do artigo é construído sobre isto. |
| `lccclass_minsarr` | float32 | minute/flight | recompute | Sum of the signed arrival delays of carriers of class LCC, Webjet included divided by the realised flights of ALL carriers on the route-month. The denominator is the article's, and it dilutes the mean by the other carriers' share (declared difference). | Soma dos atrasos de chegada com sinal empresas de classe LCC, Webjet incluída dividida pelos voos realizados de TODAS as empresas na rota-mês. O denominador é o do artigo, e dilui a média pela participação das outras empresas (diferença declarada). |
| `fscc_n` | int32 | flights | sum | Realised flights of carriers of class FSC (ADR-0003) on the route-month. | Voos realizados empresas de classe FSC (ADR-0003) na rota-mês. |
| `fscc_prdelarr` | float32 | share | recompute | Flights of carriers of class FSC (ADR-0003) arriving more than 15 minutes late, over their arrival denominator. The article's ODDS is built on this. | Voos empresas de classe FSC (ADR-0003) chegando com mais de 15 minutos de atraso, sobre o denominador de chegada. O ODDS do artigo é construído sobre isto. |
| `fscc_prdeldep` | float32 | share | recompute | Flights of carriers of class FSC (ADR-0003) departing more than 15 minutes late. | Voos empresas de classe FSC (ADR-0003) partindo com mais de 15 minutos de atraso. |
| `fscc_oddsarr` | float32 | log-odds | recompute | Log-odds of the arrival proportion of carriers of class FSC (ADR-0003); null at 0 and 1. | Log-odds da proporção de chegada empresas de classe FSC (ADR-0003); nulo em 0 e 1. |
| `fscc_minsarr` | float32 | minute/flight | recompute | Sum of the signed arrival delays of carriers of class FSC (ADR-0003) divided by the realised flights of ALL carriers on the route-month. The denominator is the article's, and it dilutes the mean by the other carriers' share (declared difference). | Soma dos atrasos de chegada com sinal empresas de classe FSC (ADR-0003) dividida pelos voos realizados de TODAS as empresas na rota-mês. O denominador é o do artigo, e dilui a média pela participação das outras empresas (diferença declarada). |
| `fscc_minsp15arr` | float32 | minute/flight | recompute | Sum of the arrival delays of carriers of class FSC (ADR-0003) counting only what exceeds 15 minutes, over the same denominator. | Soma dos atrasos de chegada empresas de classe FSC (ADR-0003) contando só o que passa de 15 minutos, sobre o mesmo denominador. |
| `lccfu_f` | int32 | flights | sum | Flights of the Gol and Azul groups, the article's LCC set on the route-month, realised plus cancelled. | Voos os grupos Gol e Azul, o conjunto LCC do artigo na rota-mês, realizados mais cancelados. |
| `lccfu_n` | int32 | flights | sum | Realised flights of the Gol and Azul groups, the article's LCC set on the route-month. | Voos realizados os grupos Gol e Azul, o conjunto LCC do artigo na rota-mês. |
| `lccfu_prdelarr` | float32 | share | recompute | Flights of the Gol and Azul groups, the article's LCC set arriving more than 15 minutes late, over their arrival denominator. The article's ODDS is built on this. | Voos os grupos Gol e Azul, o conjunto LCC do artigo chegando com mais de 15 minutos de atraso, sobre o denominador de chegada. O ODDS do artigo é construído sobre isto. |
| `lccfu_prdeldep` | float32 | share | recompute | Flights of the Gol and Azul groups, the article's LCC set departing more than 15 minutes late. | Voos os grupos Gol e Azul, o conjunto LCC do artigo partindo com mais de 15 minutos de atraso. |
| `lccfu_oddsarr` | float32 | log-odds | recompute | Log-odds of the arrival proportion of the Gol and Azul groups, the article's LCC set; null at 0 and 1. | Log-odds da proporção de chegada os grupos Gol e Azul, o conjunto LCC do artigo; nulo em 0 e 1. |
| `lccfu_minsarr` | float32 | minute/flight | recompute | Sum of the signed arrival delays of the Gol and Azul groups, the article's LCC set divided by the realised flights of ALL carriers on the route-month. The denominator is the article's, and it dilutes the mean by the other carriers' share (declared difference). | Soma dos atrasos de chegada com sinal os grupos Gol e Azul, o conjunto LCC do artigo dividida pelos voos realizados de TODAS as empresas na rota-mês. O denominador é o do artigo, e dilui a média pela participação das outras empresas (diferença declarada). |
| `all_prdelarr` | float32 | share | recompute | All carriers: realised flights arriving more than 15 minutes late, over the arrival denominator. | Todas as empresas: voos realizados chegando com mais de 15 minutos de atraso, sobre o denominador de chegada. |
| `all_prdeldep` | float32 | share | recompute | All carriers: realised flights departing more than 15 minutes late. | Todas as empresas: voos realizados partindo com mais de 15 minutos de atraso. |
| `all_minsarr` | float32 | minute/flight | recompute | Article `all_minsarr`: sum of every carrier's signed arrival delay divided by the realised flights of the route-month. | `all_minsarr` do artigo: soma dos atrasos de chegada com sinal de todas as empresas dividida pelos voos realizados da rota-mês. |
| `all_minsdep` | float32 | minute/flight | recompute | The departure counterpart of all_minsarr. | A contrapartida de partida de all_minsarr. |
| `pres_glo` | int8 | flag | recompute | The Gol group operated the route this month. The benchmark's `pres_glo` is ticket sales, not operation (declared difference). | O grupo Gol operou a rota neste mês. O `pres_glo` do gabarito é venda de bilhetes, não operação (diferença declarada). |
| `pres_azu` | int8 | flag | recompute | The Azul group operated the route this month. | O grupo Azul operou a rota neste mês. |
| `pres_tam` | int8 | flag | recompute | The TAM group operated the route this month. | O grupo TAM operou a rota neste mês. |
| `lcc` | int8 | flag | recompute | Article `lcc`: Gol or Azul present on the route. Here from operations; the benchmark reads it from the tariff base, and the two disagree on about 11% of route-months (declared difference). | `lcc` do artigo: Gol ou Azul presente na rota. Aqui por operação; o gabarito lê da base tarifária, e as duas discordam em cerca de 11% das rota-meses (diferença declarada). |
| `n_rows_all` | int32 | rows | sum | Every staged row of the route-month, any DI, line type or status — the universe is a filter, not a fact about the world. | Todas as linhas staged da rota-mês, qualquer DI, tipo de linha ou situação — o universo é um filtro, não um fato sobre o mundo. |
| `n_extra` | int32 | flights | sum | Extra flights, DI 1 and 2, outside the replication universe. | Voos extras, DI 1 e 2, fora do universo de replicação. |
| `n_return` | int32 | flights | sum | Return flights, DI 3. | Voos de retorno, DI 3. |
| `n_intl_leg` | int32 | flights | sum | Domestic legs of international lines, line type I. | Trechos domésticos de linhas internacionais, tipo de linha I. |
| `n_cargo` | int32 | flights | sum | Cargo flights, line types C and G. | Voos cargueiros, tipos de linha C e G. |
| `n_postal` | int32 | flights | sum | Postal network flights, line type L. | Voos da rede postal, tipo de linha L. |
| `n_off_universe` | int32 | rows | sum | Rows of the route-month outside the replication universe. | Linhas da rota-mês fora do universo de replicação. |
| `dep_delay_median_min` | float32 | minute | recompute | Median departure delay, recomputed from the flights of this cell (ADR-0004). | Mediana do atraso de partida, recalculada dos voos da célula (ADR-0004). |
| `dep_delay_p90_min` | float32 | minute | recompute | 90th percentile of the departure delay, recomputed from the flights. | Percentil 90 do atraso de partida, recalculado dos voos. |
| `arr_delay_median_min` | float32 | minute | recompute | Median arrival delay, recomputed from the flights of this cell (ADR-0004). | Mediana do atraso de chegada, recalculada dos voos da célula (ADR-0004). |
| `arr_delay_p90_min` | float32 | minute | recompute | 90th percentile of the arrival delay, recomputed from the flights. | Percentil 90 do atraso de chegada, recalculado dos voos. |
| `sh_extra` | float32 | share | recompute | n_extra divided by n_rows_all. | n_extra dividido por n_rows_all. |
| `rthhi_flights` | float32 | index 0-1 | recompute | Route concentration over flights, the VRA-computable counterpart of rthhi. | Concentração da rota sobre voos, a contrapartida calculável no VRA de rthhi. |
| `o_movements` | int32 | movements | recompute | Scheduled movements at the node: departures plus arrivals of the universe. Measured at the origin city of the route. | Movimentos programados no nó: partidas mais chegadas do universo. Medido na cidade de origem da rota. |
| `o_movements_realized` | int32 | movements | recompute | Movements that operated. Measured at the origin city of the route. | Movimentos que operaram. Medido na cidade de origem da rota. |
| `o_sh_cancel` | float32 | share | recompute | Cancelled flights divided by the flights of the cell. Measured at the origin city of the route. | Voos cancelados divididos pelos voos da célula. Medido na cidade de origem da rota. |
| `o_sh_dep_gt0` | float32 | share | recompute | dep_delayed_gt0 divided by the departure-delay denominator. Measured at the origin city of the route. | dep_delayed_gt0 dividido pelo denominador de atraso de partida. Medido na cidade de origem da rota. |
| `o_sh_dep_gt15` | float32 | share | recompute | dep_delayed_gt15 divided by the departure-delay denominator. Measured at the origin city of the route. | dep_delayed_gt15 dividido pelo denominador de atraso de partida. Medido na cidade de origem da rota. |
| `o_sh_arr_gt0` | float32 | share | recompute | arr_delayed_gt0 divided by the arrival-delay denominator. Measured at the origin city of the route. | arr_delayed_gt0 dividido pelo denominador de atraso de chegada. Medido na cidade de origem da rota. |
| `o_sh_arr_gt15` | float32 | share | recompute | arr_delayed_gt15 divided by the arrival-delay denominator. Measured at the origin city of the route. | arr_delayed_gt15 dividido pelo denominador de atraso de chegada. Medido na cidade de origem da rota. |
| `o_arr_delay_mean_min` | float32 | minute | recompute | Mean signed arrival delay over the denominator, outliers removed; early flights count negative (ADR-0008). Measured at the origin city of the route. | Atraso médio de chegada com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). Medido na cidade de origem da rota. |
| `o_pr_connc` | float32 | share | recompute | Article `pr_connc`: flights coded RA divided by f. RA is aircraft rotation, not passengers held for a connection as the article's label says (declared difference). Reproduces at 99.2%. Measured at the origin city of the route. | `pr_connc` do artigo: voos com código RA divididos por f. RA é conexão de aeronave, não espera de passageiros como diz o rótulo do artigo (diferença declarada). Reproduz 99,2%. Medido na cidade de origem da rota. |
| `o_prwheather` | float32 | share | recompute | Article `prwheather` (the misspelling is the article's): flights coded AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divided by f. Not only weather — its dominant code is AR, restricted airport (ADR-0005). Reproduces at 98.5%. Measured at the origin city of the route. | `prwheather` do artigo (o erro de grafia é do artigo): voos com código AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divididos por f. Não é só clima — seu código dominante é AR, aeroporto com restrições (ADR-0005). Reproduz 98,5%. Medido na cidade de origem da rota. |
| `o_n_groups` | int32 | count | recompute | Airline groups with at least one flight in the cell. Measured at the origin city of the route. | Grupos de empresas com ao menos um voo na célula. Medido na cidade de origem da rota. |
| `o_hhi_flights` | float32 | index 0-1 | recompute | Herfindahl index over the groups' shares of flights; not the article's passenger-based `rthhi` (ADR-0004). Measured at the origin city of the route. | Índice de Herfindahl sobre as participações dos grupos em voos; não é o `rthhi` do artigo, que usa passageiros (ADR-0004). Medido na cidade de origem da rota. |
| `o_sh_leader` | float32 | share | recompute | Flight share of the largest group in the cell. Measured at the origin city of the route. | Participação em voos do maior grupo da célula. Medido na cidade de origem da rota. |
| `o_sh_movements_lcc` | float32 | share | recompute | Share of the node's movements flown by carriers of class LCC (ADR-0011). Measured at the origin city of the route. | Participação dos movimentos do nó operados por empresas das empresas de baixo custo (LCC) (ADR-0011). Medido na cidade de origem da rota. |
| `o_lccfu_present` | int8 | flag | recompute | Gol or Azul operated at least one route touching this node this month. Measured at the origin city of the route. | Gol ou Azul operou ao menos uma rota que toca este nó neste mês. Medido na cidade de origem da rota. |
| `o_mov_hour_max` | float32 | movements | recompute | Busiest single day-hour of the month at this node. Measured at the origin city of the route. | Maior número de movimentos numa dia-hora do mês neste nó. Medido na cidade de origem da rota. |
| `o_congested_hours` | int32 | count | recompute | Day-hours of the month at or above the node's own p90 for the year (ADR-0007). Measured at the origin city of the route. | Dia-horas do mês em ou acima do p90 do próprio nó no ano (ADR-0007). Medido na cidade de origem da rota. |
| `o_sh_movements_congested` | float32 | share | recompute | Share of the node's monthly movements in congested day-hours; the ADR-0007 proxy for the article's `prcongested`. Measured at the origin city of the route. | Participação dos movimentos do mês do nó em dia-horas congestionadas; o proxy do ADR-0007 para o `prcongested` do artigo. Medido na cidade de origem da rota. |
| `o_n_hub_groups` | int32 | count | recompute | Groups for which this node is a hub this month (share, ratio and volume floors of vra.hub). Measured at the origin city of the route. | Grupos para os quais este nó é hub neste mês (participação, razão e pisos de volume de vra.hub). Medido na cidade de origem da rota. |
| `o_hub_max_score` | float32 | ratio | recompute | Largest hub score at the node: the highest ratio of a group's city share to its system share. Measured at the origin city of the route. | Maior escore de hub no nó: a maior razão entre a participação do grupo na cidade e sua participação no sistema. Medido na cidade de origem da rota. |
| `d_movements` | int32 | movements | recompute | Scheduled movements at the node: departures plus arrivals of the universe. Measured at the destination city of the route. | Movimentos programados no nó: partidas mais chegadas do universo. Medido na cidade de destino da rota. |
| `d_movements_realized` | int32 | movements | recompute | Movements that operated. Measured at the destination city of the route. | Movimentos que operaram. Medido na cidade de destino da rota. |
| `d_sh_cancel` | float32 | share | recompute | Cancelled flights divided by the flights of the cell. Measured at the destination city of the route. | Voos cancelados divididos pelos voos da célula. Medido na cidade de destino da rota. |
| `d_sh_dep_gt0` | float32 | share | recompute | dep_delayed_gt0 divided by the departure-delay denominator. Measured at the destination city of the route. | dep_delayed_gt0 dividido pelo denominador de atraso de partida. Medido na cidade de destino da rota. |
| `d_sh_dep_gt15` | float32 | share | recompute | dep_delayed_gt15 divided by the departure-delay denominator. Measured at the destination city of the route. | dep_delayed_gt15 dividido pelo denominador de atraso de partida. Medido na cidade de destino da rota. |
| `d_sh_arr_gt0` | float32 | share | recompute | arr_delayed_gt0 divided by the arrival-delay denominator. Measured at the destination city of the route. | arr_delayed_gt0 dividido pelo denominador de atraso de chegada. Medido na cidade de destino da rota. |
| `d_sh_arr_gt15` | float32 | share | recompute | arr_delayed_gt15 divided by the arrival-delay denominator. Measured at the destination city of the route. | arr_delayed_gt15 dividido pelo denominador de atraso de chegada. Medido na cidade de destino da rota. |
| `d_arr_delay_mean_min` | float32 | minute | recompute | Mean signed arrival delay over the denominator, outliers removed; early flights count negative (ADR-0008). Measured at the destination city of the route. | Atraso médio de chegada com sinal sobre o denominador, sem outliers; antecipações contam negativo (ADR-0008). Medido na cidade de destino da rota. |
| `d_pr_connc` | float32 | share | recompute | Article `pr_connc`: flights coded RA divided by f. RA is aircraft rotation, not passengers held for a connection as the article's label says (declared difference). Reproduces at 99.2%. Measured at the destination city of the route. | `pr_connc` do artigo: voos com código RA divididos por f. RA é conexão de aeronave, não espera de passageiros como diz o rótulo do artigo (diferença declarada). Reproduz 99,2%. Medido na cidade de destino da rota. |
| `d_prwheather` | float32 | share | recompute | Article `prwheather` (the misspelling is the article's): flights coded AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divided by f. Not only weather — its dominant code is AR, restricted airport (ADR-0005). Reproduces at 98.5%. Measured at the destination city of the route. | `prwheather` do artigo (o erro de grafia é do artigo): voos com código AI, AJ, AM, AR, RI, RM, WA, WO, WR, WT, XI, XJ, XM, XO, XS, XT divididos por f. Não é só clima — seu código dominante é AR, aeroporto com restrições (ADR-0005). Reproduz 98,5%. Medido na cidade de destino da rota. |
| `d_n_groups` | int32 | count | recompute | Airline groups with at least one flight in the cell. Measured at the destination city of the route. | Grupos de empresas com ao menos um voo na célula. Medido na cidade de destino da rota. |
| `d_hhi_flights` | float32 | index 0-1 | recompute | Herfindahl index over the groups' shares of flights; not the article's passenger-based `rthhi` (ADR-0004). Measured at the destination city of the route. | Índice de Herfindahl sobre as participações dos grupos em voos; não é o `rthhi` do artigo, que usa passageiros (ADR-0004). Medido na cidade de destino da rota. |
| `d_sh_leader` | float32 | share | recompute | Flight share of the largest group in the cell. Measured at the destination city of the route. | Participação em voos do maior grupo da célula. Medido na cidade de destino da rota. |
| `d_sh_movements_lcc` | float32 | share | recompute | Share of the node's movements flown by carriers of class LCC (ADR-0011). Measured at the destination city of the route. | Participação dos movimentos do nó operados por empresas das empresas de baixo custo (LCC) (ADR-0011). Medido na cidade de destino da rota. |
| `d_lccfu_present` | int8 | flag | recompute | Gol or Azul operated at least one route touching this node this month. Measured at the destination city of the route. | Gol ou Azul operou ao menos uma rota que toca este nó neste mês. Medido na cidade de destino da rota. |
| `d_mov_hour_max` | float32 | movements | recompute | Busiest single day-hour of the month at this node. Measured at the destination city of the route. | Maior número de movimentos numa dia-hora do mês neste nó. Medido na cidade de destino da rota. |
| `d_congested_hours` | int32 | count | recompute | Day-hours of the month at or above the node's own p90 for the year (ADR-0007). Measured at the destination city of the route. | Dia-horas do mês em ou acima do p90 do próprio nó no ano (ADR-0007). Medido na cidade de destino da rota. |
| `d_sh_movements_congested` | float32 | share | recompute | Share of the node's monthly movements in congested day-hours; the ADR-0007 proxy for the article's `prcongested`. Measured at the destination city of the route. | Participação dos movimentos do mês do nó em dia-horas congestionadas; o proxy do ADR-0007 para o `prcongested` do artigo. Medido na cidade de destino da rota. |
| `d_n_hub_groups` | int32 | count | recompute | Groups for which this node is a hub this month (share, ratio and volume floors of vra.hub). Measured at the destination city of the route. | Grupos para os quais este nó é hub neste mês (participação, razão e pisos de volume de vra.hub). Medido na cidade de destino da rota. |
| `d_hub_max_score` | float32 | ratio | recompute | Largest hub score at the node: the highest ratio of a group's city share to its system share. Measured at the destination city of the route. | Maior escore de hub no nó: a maior razão entre a participação do grupo na cidade e sua participação no sistema. Medido na cidade de destino da rota. |
| `olccfu` | int8 | flag | recompute | Article `olccfu`: Gol or Azul operated some route touching the origin city this month. | `olccfu` do artigo: Gol ou Azul operou alguma rota que toca a cidade de origem neste mês. |
| `dlccfu` | int8 | flag | recompute | Article `dlccfu`: the same at the destination city. | `dlccfu` do artigo: o mesmo na cidade de destino. |
| `maxalccfu` | int8 | flag | recompute | Article `maxalccfu`: the larger of olccfu and dlccfu. Reproduces the benchmark on 100% of route-months. | `maxalccfu` do artigo: o maior entre olccfu e dlccfu. Reproduz o gabarito em 100% das rota-meses. |
| `maxcthhi_flights` | float32 | index 0-1 | recompute | The larger of the two endpoint cities' flight HHIs. | O maior entre os HHIs de voos das duas cidades-extremo. |
| `gmchhi_flights` | float32 | index 0-1 | recompute | Geometric mean of the two endpoint cities' flight HHIs. | Média geométrica dos HHIs de voos das duas cidades-extremo. |
| `maxcongested` | float32 | share | recompute | The larger of the two endpoint cities' congestion proxies (ADR-0007). | O maior entre os proxies de congestionamento das duas cidades-extremo (ADR-0007). |
| `maxprdel_proxy` | float32 | share | recompute | Proxy for the article's `maxprdel`: the larger of the two endpoint cities' arrival-delay rates. The article's own definition was not recovered (over 40 candidates tested) and is not claimed here. | Proxy para o `maxprdel` do artigo: a maior das taxas de atraso de chegada das duas cidades-extremo. A definição do artigo não foi recuperada (mais de 40 candidatas testadas) e não é reivindicada aqui. |
| `rthhi` | float32 | index 0-1 | recompute | Article `rthhi`: route concentration over paid passengers. Null throughout: the VRA has no traffic and ANAC's statistical data are not collected (ADR-0004). See rthhi_flights. | `rthhi` do artigo: concentração da rota sobre passageiros pagos. Nulo em toda a base: o VRA não tem tráfego e os dados estatísticos da ANAC não foram coletados (ADR-0004). Ver rthhi_flights. |
| `maxcthhi` | float32 | index 0-1 | recompute | Article `maxcthhi`: the larger endpoint-city passenger HHI. Null for the same reason as rthhi. | `maxcthhi` do artigo: o maior HHI de passageiros entre as cidades-extremo. Nulo pelo mesmo motivo de rthhi. |
| `gmchhi` | float32 | index 0-1 | recompute | Article `gmchhi`: geometric mean of the two endpoint-city passenger HHIs. Null for the same reason. | `gmchhi` do artigo: média geométrica dos dois HHIs de passageiros das cidades-extremo. Nulo pelo mesmo motivo. |
| `prcongested` | float32 | share | recompute | Article `prcongested`: share of flights in a clock hour above the airport's declared capacity. Null: capacity.csv holds one airport, and one row is not a panel (ADR-0007). The p90 proxy is o_/d_sh_movements_congested. | `prcongested` do artigo: participação dos voos em hora cheia acima da capacidade declarada do aeroporto. Nulo: capacity.csv tem um aeroporto, e uma linha não é um painel (ADR-0007). O proxy p90 é o_/d_sh_movements_congested. |
| `legacy_missing_actual_as_zero` | int8 | flag | none | The ADR-0012 convention this table was built under: 1 means a realised flight with no actual time counted as on schedule, which is what reproduces the benchmark. | A convenção do ADR-0012 sob a qual esta tabela foi construída: 1 significa que um voo realizado sem horário real contou como pontual, que é o que reproduz o gabarito. |
