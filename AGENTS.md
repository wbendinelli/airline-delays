# Agent instructions

Read [CLAUDE.md](CLAUDE.md) — the operating manual for any coding agent
working in this repository, regardless of tool. In one sentence: no column
exists without a `src/vra/registry.py` entry, no number in prose exists
without a versioned script that prints it, and a red CI gets fixed at the
cause, never at the check.
