# Agent instructions

Read [CLAUDE.md](CLAUDE.md) -- the operating manual for any coding agent
working in this repository, regardless of tool. In one sentence: no column
exists without an entry in `src/airline_delays/schema/columns.py`, no number
in the entry pages exists that is not a value of `reports/summary.json` or
marked as an outside document, and a red CI gets fixed at the cause, never at
the check.
